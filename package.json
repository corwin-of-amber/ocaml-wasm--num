{
    "name": "@ocaml-wasm/4.08--num",
    "version": "1.4.0",
    "description": "The legacy Num library for arbitrary-precision integer and rational arithmetic (WASM port)",
    "scripts": {
        "prepack": "rm -rf bin && mkdir -p bin && cp dllnums.wasm bin/"
    },
    "files": ["bin"],
    "repository": "https://github.com/corwin-of-amber/ocaml-wasm--num.git"
}
